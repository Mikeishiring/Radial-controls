{
  "name": "radial-controls",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone design prototype for weighted radial controls and semantic mark composition.",
  "scripts": {
    "build": "node scripts/build-public.js",
    "start": "node server.js",
    "verify": "node scripts/verify-cdp.js",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name radial-controls --branch main --commit-hash local-demo --commit-message \"Public Radial Controls design demo\" --commit-dirty=true"
  }
}
